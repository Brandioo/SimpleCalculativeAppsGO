from CountZeros import get_zeros

# write a function that takes a list a parameter
# return the max count of consequent 0-s

assert get_zeros([1, 2, 3, 4, 5, 0]) == 1

assert get_zeros([0, 0, 0, 4, 5, 0]) == 3
